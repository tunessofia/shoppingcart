import React from 'react';

export const Cart = () => {
    
    return (
        <Fragment>
            <div style={styles.fixed}>
                <div>
                    <h2>{'Cart'}</h2>
                    {/* inserir tabela com cervejas compradas + botoes de acoes*/} 
                </div>
                <div>{/* total do valor */}</div>
                {/* botao voltar (opcional, use o back do browser) + botao encomendar */}
                {/* botao voltar vai para as cervejas */}
            </div>
        </Fragment> 
    );
}